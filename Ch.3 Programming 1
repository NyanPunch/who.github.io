사용자로부터 세 개의 실수를 입력받은 후, 합계와 평균값을 계산하여 화면에 출력하는 프로그램

#코드
#include <stdio.h>
int main(void) {
	float a, b, c, sum, avg;
	sum = 0.0;
	avg = 0.0;

	printf("실수를 입력하세요: ");
	scanf("%f", &a);
	printf("실수를 입력하세요: ");
	scanf("%f", &b);
	printf("실수를 입력하세요: ");
	scanf("%f", &c);
	sum = a + b + c;
	avg = sum / 3;
	printf("합: %f, 평균: %f", sum, avg);

	return 0;
}
